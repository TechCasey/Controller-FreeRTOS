/**
 * @file    simple_example.h
 * @brief   FreeRTOS 最简示例 — 头文件
 *
 * 展示一个最基础的 FreeRTOS 任务：周期性打印日志 + LED 闪烁。
 * 这是学习 FreeRTOS 的起点，理解后再看 CanComm.c。
 */

#ifndef SIMPLE_EXAMPLE_H_
#define SIMPLE_EXAMPLE_H_

#include "include.h"

/* ============================================================
 * 【核心自定义区】任务栈配置
 * ─────────────────────────────────────────────────────────────
 * configMINIMAL_STACK_SIZE = 128 (word) = 512 字节
 * 这里用 2 倍，即 256 word = 1KB
 * 如果任务里有很多局部变量，可以增大到 3~4 倍
 * ============================================================ */
#define SIMPLE_TASK_STACK  (configMINIMAL_STACK_SIZE * 2)

/* ============================================================
 * 【核心自定义区】任务优先级配置
 * ─────────────────────────────────────────────────────────────
 * osPriorityNormal = 7（最常用的优先级）
 * 不要和系统任务（如定时器守护）冲突即可
 * ============================================================ */
#define SIMPLE_TASK_PRIORITY  osPriorityNormal

/* ============================================================
 * 【核心自定义区】定时器周期配置
 * ─────────────────────────────────────────────────────────────
 * configTICK_RATE_HZ = 1000，所以 1 tick = 1ms
 * 1000 tick = 1000ms = 1秒
 * ============================================================ */
#define SIMPLE_TIMER_PERIOD_TICK  1000  /* 定时器周期：1000 ticks = 1秒 */

/* ============================================================
 * 函数声明
 * ============================================================ */
/**
 * 简单示例任务初始化
 * 在 main.c 的 Step 12（核心自定义区）调用
 *
 * 创建内容：
 *   1. 简单任务（SimpleTask）— 打印状态 + LED 闪烁
 *   2. 定时器（SimpleTimer）— 1秒周期，自动重载
 */
void SimpleTaskInit(void);

#endif /* SIMPLE_EXAMPLE_H_ */